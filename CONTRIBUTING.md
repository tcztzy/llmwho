# Contributing

LLMWho welcomes focused issues and pull requests. Before adding a detector,
state what evidence it uses, when it can fail, and how uncertainty is exposed.
Never add telemetry that persists credentials or raw content by default.

Development and test commands will be documented as the Python and Node
packages land. Changes to public behavior should update `SPEC.md`, relevant
tests, and user documentation together.
