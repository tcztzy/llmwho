{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://llmwho.dev/schema/observation-v2.json",
  "title": "LLMWho ObservationV2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "event_id",
    "timestamp",
    "source",
    "modality",
    "sdk",
    "endpoint",
    "transport",
    "identity",
    "privacy"
  ],
  "properties": {
    "schema_version": { "const": "2" },
    "event_id": { "type": "string", "minLength": 1 },
    "timestamp": { "type": "string", "format": "date-time" },
    "source": { "enum": ["passive", "probe"] },
    "modality": {
      "enum": ["text", "image", "audio", "embedding", "multimodal", "unknown"]
    },
    "sdk": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "version"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "version": { "type": "string", "minLength": 1 }
      }
    },
    "endpoint": {
      "type": "object",
      "additionalProperties": false,
      "required": ["scheme", "host", "path"],
      "properties": {
        "scheme": { "enum": ["http", "https", "unknown"] },
        "host": { "type": "string" },
        "port": { "type": "integer", "minimum": 1, "maximum": 65535 },
        "path": { "type": "string" },
        "provider": { "type": "string" }
      }
    },
    "request": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "operation": { "type": "string" },
        "requested_model": { "type": "string" },
        "stream": { "type": "boolean" },
        "input_bytes": { "type": "integer", "minimum": 0 },
        "role_count": { "type": "integer", "minimum": 0 },
        "probe_case_id": { "type": "string" }
      }
    },
    "response": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "system_fingerprint": { "type": "string" },
        "status_code": { "type": "integer", "minimum": 100, "maximum": 599 },
        "output_bytes": { "type": "integer", "minimum": 0 },
        "usage": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "input_tokens": { "type": "integer", "minimum": 0 },
            "output_tokens": { "type": "integer", "minimum": 0 },
            "total_tokens": { "type": "integer", "minimum": 0 }
          }
        }
      }
    },
    "model_declaration": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "declared_model", "evidence"],
      "properties": {
        "status": { "enum": ["matched", "mismatch", "unverified"] },
        "declared_model": { "type": "string", "minLength": 1 },
        "evidence": {
          "type": "array",
          "minItems": 1,
          "maxItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kind", "source", "value"],
            "properties": {
              "kind": { "const": "provider_declaration" },
              "source": { "const": "response.body.model" },
              "value": { "type": "string", "minLength": 1 }
            }
          }
        }
      }
    },
    "transport": {
      "type": "object",
      "additionalProperties": false,
      "required": ["outcome", "duration_ms"],
      "properties": {
        "outcome": {
          "enum": ["success", "http_error", "timeout", "network_error", "stream_error"]
        },
        "duration_ms": { "type": "number", "minimum": 0 },
        "ttft_ms": { "type": "number", "minimum": 0 },
        "error_type": { "type": "string" }
      }
    },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "candidates", "evidence"],
      "properties": {
        "status": { "enum": ["unknown", "inferred"] },
        "candidates": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["label", "confidence"],
            "properties": {
              "label": { "type": "string", "minLength": 1 },
              "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
            }
          }
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kind", "source", "detector_id", "detector_version"],
            "properties": {
              "kind": { "type": "string" },
              "source": { "type": "string" },
              "value": { "type": "string" },
              "detector_id": { "type": "string", "minLength": 1 },
              "detector_version": { "type": "string", "minLength": 1 }
            }
          }
        }
      }
    },
    "probe": {
      "type": "object",
      "additionalProperties": false,
      "required": ["case_id", "passed", "score"],
      "properties": {
        "case_id": { "type": "string" },
        "passed": { "type": "boolean" },
        "score": { "type": "number", "minimum": 0, "maximum": 1 },
        "check": { "type": "string" }
      }
    },
    "privacy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["content_captured", "redactions"],
      "properties": {
        "content_captured": { "const": false },
        "redactions": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
